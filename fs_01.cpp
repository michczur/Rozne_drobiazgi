/*
 * Napisz program, który utworzy listę (cokolwiek to znaczy) nazw plików,
 * i katalogów oraz wypisze te nazwy na stdout (std::cout) w taki sposób,
 * żeby było widać co jest plikiem, a co katalogiem.
 */

int main()
{
	//dodaj plik: test.txt
	//dodaj plik: img.jpg
	//dodaj katalog: Italia
	//dodaj plik: zdj.png

	dodaj_plik("test.txt");
	obj.dodaj_plik("test.txt");

	std::vector <std::string> nazwy;
	nazwy.push_back("test.txt");

	return 0;
}
